#include "stdafx.h"
#include <iostream>
#include "PolygonGenerator.h"

using namespace std;
/*
int main()
{

	PolygonGenerator p;

	//graph1 : case 1
	vector<Triangle> gen_very_large = p.Generate(very_large,10);
	vector<Triangle> gen_large = p.Generate(large,10);
	vector<Triangle> gen_medium = p.Generate(medium,10);
	vector<Triangle> gen_small = p.Generate(small,10);
	vector<Triangle> gen_very_small = p.Generate(very_small,10);



	//graph2 : case 2
	vector<Triangle> triangles_covered_by_each_pixel = p.Generate(10);
	return 1;
}*/